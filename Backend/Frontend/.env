# Hotel Intelligence — API base URL
# During local dev Vite proxy handles it automatically (leave empty or keep localhost).
# Change this to your deployed App Engine URL before production build.
VITE_API_URL=http://localhost:8000

# Anthropic API Key — get it from: https://console.anthropic.com/settings/keys
VITE_ANTHROPIC_KEY="your_key_here"