runtime: python312
instance_class: F2

entrypoint: uvicorn main:app --host 0.0.0.0 --port $PORT

env_variables:
  PYTHONUNBUFFERED: "1"

handlers:
  - url: /.*
    script: auto
    secure: always

automatic_scaling:
  min_instances: 1
  max_instances: 5
  target_cpu_utilization: 0.65
